/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'klrfpzxjsacriaqtfssf.supabase.co',
      },
    ],
  },
}

export default nextConfig
